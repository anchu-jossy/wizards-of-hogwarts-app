<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".WizardsOfHogwartsApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/hogwartz"
        android:label="@string/app_name"
        android:roundIcon="@drawable/hogwartz"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThemeSwitcher"
        tools:targetApi="34" >

    <activity
        android:name="com.example.wizardsofhogwarts.presentation.MainActivity"
        android:exported="true"
        android:theme="@style/Theme.ThemeSwitcher">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

    </application>
</manifest>